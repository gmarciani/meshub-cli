[tox]
envlist = test,lint,type,coverage

[testenv]
deps = {[dev]deps}

[dev]
deps =
    pytest~=9.0
    pytest-cov~=7.0
    flake8~=7.3
    black~=25.11
    autoflake~=2.3
    mypy~=1.18
    types-PyYAML~=6.0
    types-requests~=2.32

[testenv:test]
commands = pytest tests/ -v --cov=meshub --cov-report=term-missing

[testenv:lint]
commands =
    flake8 src/meshub tests/
    black --check src/meshub tests/

[testenv:type]
commands = mypy src/meshub

[testenv:format]
commands =
    autoflake --in-place --remove-all-unused-imports --recursive src/meshub tests/
    black src/meshub tests/

[testenv:coverage]
commands = pytest tests/ -v --cov=meshub --cov-report=html --cov-report=term-missing

[flake8]
max-line-length = 88
extend-ignore = E203
